version: '3'

includes:
  build:
    taskfile: ./build.yml
    internal: true


tasks:
  fmt:
    desc: Format code using Ruff
    deps: ['build:uv']
    cmds:
      - uvx ruff format .

  lint:
    desc: Run pre-commit hooks
    deps: ['build:uv']
    cmds:
      - uvx pre-commit run --all-files

  deptry:
    desc: Check for dependency issues
    deps: ['build:uv']
    cmds:
      - |
        if [ -f "pyproject.toml" ]; then

          uvx deptry $SOURCE_FOLDER {{.OPTIONS}}
        else
          printf "${YELLOW}[WARN] No pyproject.toml found, skipping deptry${RESET}\n"
        fi

  check:
    desc: Run all code quality checks
    cmds:
      - task: fmt
      - task: lint
      - task: deptry
      - cmd: printf "${GREEN}[INFO] All checks passed!${RESET}\n"
